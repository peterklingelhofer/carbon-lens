# Carbon Mesh Control Plane — Environment Configuration
# Copy to .env and fill in your API keys

# Carbon data source: "hybrid" (recommended), "mock", "eia", "gridstatus", "electricity_maps"
# "hybrid" cascades: UK → EIA → AEMO → Grid India → ONS Brazil → Eskom → GridStatus → ENTSO-E → Open-Meteo → Mock
CARBON_MESH_CARBON_SOURCE=hybrid

# EIA API (free — https://www.eia.gov/opendata/)
CARBON_MESH_EIA_API_KEY=

# GridStatus.io (free tier — https://www.gridstatus.io/)
CARBON_MESH_GRID_STATUS_API_KEY=

# ENTSO-E Transparency Platform (free token — https://transparency.entsoe.eu/)
CARBON_MESH_ENTSOE_TOKEN=

# Electricity Maps (optional, paid — https://api-portal.electricitymaps.com/)
CARBON_MESH_ELECTRICITY_MAPS_API_KEY=

# Cache TTL in seconds (default: 300 = 5 minutes)
CARBON_MESH_CACHE_TTL_SECONDS=300

# Server
CARBON_MESH_HOST=0.0.0.0
CARBON_MESH_PORT=8000

# CORS origins (comma-separated in env, or JSON list)
CARBON_MESH_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# PostgreSQL — set USE_DATABASE=true to enable persistence
CARBON_MESH_USE_DATABASE=false
CARBON_MESH_DATABASE_URL=postgresql+asyncpg://carbon_mesh:carbon_mesh@localhost:5432/carbon_mesh

# Auth — set to true to require API keys on all routes
CARBON_MESH_API_KEY_REQUIRED=false

# Admin secret — required for /api/v1/admin/* endpoints (API key management)
CARBON_MESH_ADMIN_SECRET=

# Rate limiting
CARBON_MESH_RATE_LIMIT_DEFAULT=100/minute
CARBON_MESH_RATE_LIMIT_ROUTE=30/minute

# Logging — "text" for dev, "json" for production (Datadog, ELK, CloudWatch)
CARBON_MESH_LOG_FORMAT=text
CARBON_MESH_LOG_LEVEL=INFO

# Startup — auto-run Alembic migrations (recommended for PaaS deploys)
CARBON_MESH_AUTO_MIGRATE=false

# Request limits — max body size in bytes (default: 1 MB)
CARBON_MESH_MAX_REQUEST_BODY_BYTES=1048576

# Stripe billing (optional — leave empty to disable)
# Get keys at https://dashboard.stripe.com/apikeys
CARBON_MESH_STRIPE_SECRET_KEY=
CARBON_MESH_STRIPE_WEBHOOK_SECRET=
CARBON_MESH_STRIPE_PRICE_ID_PRO=
CARBON_MESH_STRIPE_PRICE_ID_ENTERPRISE=
