# Default (local dev) environment values
database:
  bundled:
    enabled: true
    auth:
      postgresPassword: carbonlens
      database: carbonlens

carbonProviders:
  eiaApiKey: ""
  gridStatusApiKey: ""
  entsoeToken: ""
  electricityMapsApiKey: ""

config:
  carbonSource: hybrid
  apiKeyRequired: false
  logFormat: text
  logLevel: DEBUG
  autoMigrate: true
